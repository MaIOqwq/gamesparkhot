[database]
host = ${DB_HOST:localhost}
port = ${DB_PORT:3306}
user = ${DB_USER:spark}
password = ${DB_PASSWORD:your_db_password}
database = ${DB_NAME:standardized_data}
connect_timeout = 10
read_timeout = 300
write_timeout = 300

[log]
level = INFO
format = %(asctime)s - %(levelname)s - %(message)s